<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="proyects.camachopichal.apps.anotherweatherapp">

    <!-- Permisos de la app -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <!-- PERMISOS PARA LA CÁMARA -->
    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Permiso de escritura (solo necesario para versiones antiguas de Android, opcional) -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AnotherWeatherApp"
        android:usesCleartextTraffic="true">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyC79a9ld-7xKMwOGKEa-2GP7qgnsX0tZm4" />

        <!-- CONFIGURACIÓN DEL FILE PROVIDER (Obligatorio para guardar la foto) -->
        <!-- Esto permite que la cámara guarde la imagen en tu app de forma segura -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"
                    />

        </provider>

        <!-- LoginActivity -->
        <activity
            android:name=".activities.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- RegisterActivity -->
        <activity
            android:name=".activities.RegisterActivity"
            android:exported="false" />

        <!-- MainActivity (pantalla principal)-->
        <activity
            android:name=".activities.MainActivity"
            android:exported="false" />

        <!-- MapSelectionActivity (Seleccion de ubicacion en el mapa, ajena a la actual) -->
        <activity
            android:name=".activities.MapSelectionActivity"
            android:exported="false" />

        <!-- HourlyForecastActivity (Pronostico por hora) -->
        <activity
            android:name=".activities.HourlyForecastActivity"
            android:exported="false" />

        <!-- DetailedWeatherActivity (Detalles del clima por hora) -->
        <activity
            android:name=".activities.DetailedWeatherActivity"
            android:exported="false" />
    </application>

</manifest>