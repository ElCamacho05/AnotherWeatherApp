<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Fragmento del Mapa de Google -->
    <fragment
        android:id="@+id/map"
        android:name="com.google.android.gms.maps.SupportMapFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- Marcador central fijo (opcional, para estilo Uber/Instagram) -->
    <!-- O podemos usar un marcador que se mueve al tocar, lo haremos por código -->

    <!-- Botón de Confirmar Selección -->
    <Button
        android:id="@+id/btnConfirmarUbicacion"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_margin="16dp"
        android:text="Confirmar Ubicación"
        android:backgroundTint="#00AEEF"
        android:textColor="#FFFFFF"
        android:textStyle="bold" />

</RelativeLayout>